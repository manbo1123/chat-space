.side-bar
  .side-bar__user-name
    .side-bar__user-name__name
      = current_user.name
    .side-bar__user-name__icons
      = link_to new_group_path do
        = icon('fas', 'edit', class:"side-bar__icon")
        %span<>
      = link_to edit_user_path(current_user) do
        = icon('fas', 'cog', class:"side-bar__icon")
  .side-bar__group-list
    - current_user.groups.each do |group|
      = link_to "#" do
        .side-bar__group-list__name
          = group.name
        .side-bar__group-list__message
          まだメッセージがありません
